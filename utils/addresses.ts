import { ALL_ENVIRONMENTS, ENV_ADDRESS, ENV_ENVIRONMENT } from "@utilities/defaultEnvs";

// Added addresses here as constants
// f.e.
// Frontpage is usually root path (/) so something like this:
// export const FRONTPAGE: string = ENV_ADDRESS;
//
// If login page is at /login:
// export const LOGIN: string = ENV_ADDRESS + "login";
//
// Set ENV_ADDRESS in defaultEnvs.ts
